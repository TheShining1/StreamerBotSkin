<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             xmlns:vm="using:StreamerBotSkin.ViewModels"
             x:DataType="vm:ActionsHistoryViewModel"
             xmlns:m="using:StreamerBotSkin.Models"
             x:Class="StreamerBotSkin.Views.ActionsHistoryView">
  <UserControl.DataContext>
    <vm:ActionsHistoryViewModel/>
  </UserControl.DataContext>
  <Design.DataContext>
    <vm:ActionsHistoryViewModel/>
  </Design.DataContext>
  <DataGrid ItemsSource="{Binding ActionsHistory}"
            CanUserResizeColumns="True"
            CanUserReorderColumns="True"
            HorizontalScrollBarVisibility="Visible"
            VerticalScrollBarVisibility="Visible">
    <DataGrid.Columns>
      <DataGridTextColumn Header="Name"
                          Binding="{Binding Name}"
                          x:DataType="m:PendingAction"/>
      <DataGridTextColumn Header="State"
                          Binding="{Binding State}"
                          x:DataType="m:PendingAction"/>
      <DataGridTextColumn Header="Arguments"
                          Binding="{Binding Arguments.Count}"
                          x:DataType="m:PendingAction"/>
      <DataGridTextColumn Header="Duration"
                          Binding="{Binding Duration}"
                          x:DataType="m:PendingAction"/>
      <DataGridTextColumn Header="Queue"
                          Binding="{Binding Queue}"
                          x:DataType="m:PendingAction"/>
      <DataGridTextColumn Header="QueuedAt"
                          Binding="{Binding QueuedAt}"
                          x:DataType="m:PendingAction"/>
      <DataGridTextColumn Header="StartedAt"
                          Binding="{Binding StartedAt}"
                          x:DataType="m:PendingAction"/>
      <DataGridTextColumn Header="Completed At"
                          Binding="{Binding CompletedAt}"
                          x:DataType="m:PendingAction"/>
    </DataGrid.Columns>
  </DataGrid>
</UserControl>
